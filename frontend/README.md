# Legacy Frontend Prototypes

> **Note**: This folder contains early static HTML/JS prototypes.
> The active, production-ready React application for ShopEase is located in the [`client/`](../client) directory.
